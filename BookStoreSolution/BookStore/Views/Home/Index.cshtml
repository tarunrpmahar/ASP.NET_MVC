@{
    Layout = "_Layout";
    ViewData["Title"] = "Home Page";
}

@section style{
    <link href="~/css/StyleSheet.css" rel="stylesheet" />
}

@*<link href="~/css/StyleSheet.css" rel="stylesheet" />*@
<environment names="Production, Staging">
    <h1>Production</h1>
</environment>

<section class="jumbotron text-center">
    <div class="container">
        <h1>Welcome to BookStore</h1>
        <p class="lead text-muted">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s,
            when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into
        </p>
        <p>
            <a href="#" class="btn btn-primary my-2">Tarun Singh Mahar</a>   @*Search Books*@
        </p>
    </div>
</section>

<div class="py-5 bg-light">
    <div class="container">
        @*<h3 class="h3">Top books</h3>*@
        <big>Top Books</big>    @*check this tag using inspect and big is overriden in BigTagHelper.cs*@
        @*<p big>Top book using attribute</p>*@

        @*For view components

            one way  @await Component.InvokeAsync("TopBooks", new { count = 3 })
            another way <vc:top-books></vc:top-books>*@

        <vc:top-books count="5"></vc:top-books>

        <!--<div class="row">
            <div class="col-md-3">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@Html.Partial("_bookThumbnail", new BookModel())*@
            <!--</div>
            <div class="col-md-3">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@await Html.PartialAsync("_bookThumbnail", new BookModel())*@
            <!--</div>
            <div class="col-md-3">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@{
                        Html.RenderPartial("_bookThumbnail", new BookModel());
                    }*@
            <!--</div>
            <div class="col-md-3">
                <partial name="_bookThumbnail" model="new BookModel()" />-->
                @*@{
                        await Html.RenderPartialAsync("_bookThumbnail", new BookModel());
                    }*@
            <!--</div>
        </div>-->
    </div>
</div>

@section breadcrumb{
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="#" class="tarun">Home</a></li>
            <li class="breadcrumb-item"><a href="#">Page1</a></li>
            <li class="breadcrumb-item active" aria-current="page">Index</li>
        </ol>
    </nav>
}

@section tarun{
    <script>
        $(document).ready(function () {
            //alert("Index page is ready")
        })
    </script>
}


